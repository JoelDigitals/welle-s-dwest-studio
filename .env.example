# Kostenloser API-Key aus https://aistudio.google.com/apikey
# Nötig für Moderationstexte/Nachrichten. Für die Sprachausgabe optional – ohne Key
# (oder bei Überlastung) springt automatisch der kostenlose Edge-TTS-Fallback ein.
GEMINI_API_KEY=

# Optional, aber empfohlen: kostenloser Fallback, falls Gemini überlastet ist
# oder das Freikontingent ausgeschöpft. Key unter https://console.groq.com/keys
GROQ_API_KEY=

# Optional: Modelle überschreiben, falls Google/Groq ein Modell abkündigt.
# GEMINI_TEXT_MODEL=gemini-flash-latest
# GEMINI_TTS_MODEL=gemini-3.1-flash-tts-preview
# GROQ_TEXT_MODEL=llama-3.3-70b-versatile

# Postgres-Datenbank für Accounts & geplante Sendetermine (Supabase-Projekt,
# Connection Pooling -> Transaction-Modus, Port 6543). Ohne diesen Wert
# funktioniert der Login nicht (fällt NICHT auf eine lokale Datei zurück,
# damit Accounts nie wie die alte Mediathek bei einem Redeploy verloren gehen).
DATABASE_URL=

# Zufällige Zeichenkette, mind. 32 Zeichen, zum Verschlüsseln der Login-Session.
# Einmal generieren (z. B. `openssl rand -hex 32`) und dauerhaft so lassen –
# ein Wechsel loggt alle Nutzer:innen aus.
SESSION_SECRET=
